/**
 * User types on the platform.
 * Add new types here when expanding (e.g. 'admin').
 */
const USER_TYPES = Object.freeze({
  BUSINESS: "business",
  EXPERT: "expert",
});

module.exports = { USER_TYPES };
